<% @contact.each do |info| %>
  <h1> <%= info.title %> </h1>
  <p> <%= info.phone_number %> </p>
  <p> <%= info.fax_number %> </p>
  <p> <%= info.email %> </p>
  <p> <%= info.address %> </p>
  <div><%= image_tag info.main_img %></div>
<% end %>